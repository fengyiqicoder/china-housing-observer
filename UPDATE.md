# 月度数据更新手册（供自动化任务执行）

本项目每月 1 日自动更新一次 `data.js` 中的城市数据。执行方（Claude 定时任务）按以下流程操作。

## 流程

1. **联网查询最新数据**，逐项核对 `data.js` 中 307 个中国城市与 4 个世界参照城市。城市多，可按省份分批查询；小城市若查不到平台挂牌数据，参考同省同级别城市水平微调或保持原值：
   - `price` 平均房价（元/㎡）：以贝壳找房、安居客、诸葛找房等平台公布的**二手住宅挂牌均价**为准，多来源交叉核对，取整到百位。
   - `rent` 平均月租（元/月）：以上述平台**整租套均月租金**为准，取整到五十位。
   - `salary` 平均工资（元/月，税前）：以国家统计局及各地统计局最新发布的**城镇单位就业人员平均工资**为准，年薪 ÷ 12 折算为月薪，取整到百位。
   - 台湾城市（tier 为「台湾」的 9 城）：以不动产实价登录、591 房屋交易网等公开数据为准，薪资以当地统计部门发布为准，按当期新台币汇率折合人民币。
   - 世界参照城市（纽约/伦敦/东京/新加坡）：以 Numbeo 等公开数据为准，按当期汇率折合人民币。
2. **只改数字，不改结构**：不增删城市、不改字段名、不动 `assumptions`。查不到可靠新数据的城市保持原值不动。
3. **更新数据版本**：把 `data.js` 中 `updatedAt` 改为 `"YYYY 年 M 月核对（多平台挂牌/统计数据估算值）"`（用执行当天日期）。
4. **本地验证**：确认 `data.js` 仍是合法 JS（`node --check data.js`），城市数量仍为 307 + 4。
5. **提交并推送**：
   ```
   git add data.js
   git commit -m "chore: YYYY-MM 月度数据更新"
   git push origin main
   ```
   推送到 main 后 GitHub Pages（fangjia.000ooo.ooo）自动生效。

## 注意

- 所有数值为估算整理值，页面页脚已注明口径，无需在页面上另加说明。
- 若某月来源数据整体缺失（如平台改版），跳过该月更新并保持原数据，不要写入猜测值。
